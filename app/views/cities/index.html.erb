<div class="cities">
  <h1>Cidades</h1>
  <table class="cities-table">
    <thead>
    <tr>
      <th>Nome da Cidade</th>
    </tr>
    </thead>
    <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= city.name %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>